# Raw experiment data (json, jsonl, csv, txt) are not tracked
results/*/data/
results/**/*.json
results/**/*.jsonl
*.log
*.tmp

# Go build artifacts
autonat-node
testbed/autonat

# Synced go-libp2p for Docker builds (created by run.sh)
testbed/go-libp2p-patched/

# Local-only docs
docs/nat-classification-crawl-idea.md
